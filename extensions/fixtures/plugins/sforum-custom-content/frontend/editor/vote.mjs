// Prebuilt Tiptap L2 module for the custom-content reference vote node.
// Host binds this package path + digest; the module does not execute server code.
export default {
  name: "customContentVote",
  group: "block",
  atom: true,
  selectable: true,
};
