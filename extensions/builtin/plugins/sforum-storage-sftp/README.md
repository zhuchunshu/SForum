# sforum.storage-sftp

Protected built-in SFTP storage provider. Enable it, configure its own
settings, select `plugin:sforum.storage-sftp` in Attachment settings, test the
connection, then upload. A matching SSH host-key SHA256 fingerprint is required.
