import type { AdminComponentMetadata, AdminExtensionLocaleMessages } from './types'

export const releaseId = 'core'
export const reloadMode = 'prompt'
export const contributions: readonly AdminComponentMetadata[] = Object.freeze([])
export const locales: AdminExtensionLocaleMessages = Object.freeze({})
