# 2026-07-04 I18n Base URL Fix

## Changed

- Wired `APP_URL` into the web Compose service.
- Set Nuxt site config and Nuxt i18n `baseUrl` from `APP_URL`.
- Added `NUXT_PUBLIC_I18N_BASE_URL` so runtime config can override the value in
  Docker-built production images.
- Made `scripts/dev.sh` add `APP_URL=http://127.0.0.1:3000` to older local
  `.env` files when missing, and migrate the previous local default from
  `localhost` to `127.0.0.1`.

## Decisions

- Reused the existing `APP_URL` setting instead of adding a separate site URL
  concept.

## Next

- Update `.env` manually if `WEB_PORT` is changed from `3000`, so `APP_URL`
  matches the browser-facing address.

## Open Questions

- None.
