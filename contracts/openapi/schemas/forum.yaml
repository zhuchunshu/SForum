ApiResponseCategoryList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/Category"
ApiResponseCategory:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/Category"
ApiResponseCategoryGroup:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/CategoryGroup"
ApiResponseCategoryGroupList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/CategoryGroup"
ApiResponseTag:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/Tag"
ApiResponseTagList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/Tag"
ApiResponseForumSettings:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ForumSettings"
ApiResponseTopicList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/TopicList"
ApiResponseSearchOutput:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/SearchOutput"
ApiResponseTopicDetail:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/TopicDetail"
ApiResponseComment:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/Comment"
ApiResponseCommentArray:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/Comment"
ApiResponseCommentList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/CommentList"
ApiResponseForumRevisionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ForumRevisionList"
ApiResponseForumRevisionDetail:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ForumRevisionDetail"
ApiResponseAdminForumContentList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/AdminForumContentList"
ApiResponseAdminForumTopicDetail:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/AdminForumTopicDetail"
ApiResponseAdminForumCommentDetail:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/AdminForumCommentDetail"
ContentInput:
  type: object
  required:
  - rawContent
  properties:
    rawContent:
      type: string
    sourceFormat:
      "$ref": "#/SourceFormat"
      default: markdown
    editorType:
      type: string
      default: markdown
    editorVersion:
      type: string
      default: ''
    attachmentIds:
      type: array
      maxItems: 100
      description: Omitted preserves existing references on edit; an explicit empty array removes all content attachment references.
      items:
        type: integer
        format: int64
        minimum: 1
RenderedContent:
  type: object
  required:
  - id
  - rawContent
  - htmlContent
  - plainText
  - excerpt
  - sourceFormat
  - editorType
  - renderVersion
  - contentHash
  properties:
    id:
      type: integer
      format: int64
    rawContent:
      type: string
    htmlContent:
      type: string
    plainText:
      type: string
    excerpt:
      type: string
    sourceFormat:
      "$ref": "#/SourceFormat"
    editorType:
      type: string
    editorVersion:
      type: string
    renderVersion:
      type: string
    contentHash:
      type: string
SourceFormat:
  type: string
  enum:
  - markdown
  - html
  - editor-document
  - json
TopicStatus:
  type: string
  enum:
  - active
  - locked
  - hidden
  - deleted
CommentStatus:
  type: string
  enum:
  - active
  - hidden
  - deleted
TagStatus:
  type: string
  enum:
  - active
  - pending
  - disabled
TagCreationMode:
  type: string
  enum:
  - controlled
  - review
  - open
CategoryGroup:
  type: object
  required:
  - id
  - slug
  - name
  - description
  - visibility
  - position
  - categories
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
    slug:
      type: string
    name:
      type: string
    description:
      type: string
    visibility:
      type: string
      enum:
      - public
      - hidden
    position:
      type: integer
    categories:
      type: array
      items:
        "$ref": "#/Category"
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
Category:
  type: object
  required:
  - id
  - groupId
  - groupSlug
  - groupName
  - slug
  - name
  - description
  - icon
  - iconColor
  - visibility
  - position
  - defaultSort
  - topicCount
  - commentCount
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
    groupId:
      type: integer
      format: int64
    groupSlug:
      type: string
    groupName:
      type: string
    slug:
      type: string
    name:
      type: string
    description:
      type: string
    icon:
      type: string
      description: Nuxt Icon name such as i-tabler-message-circle. Empty string means no custom icon.
    iconColor:
      type: string
      description: Hex color such as '#0f766e'. Empty string means use the admin theme accent.
    visibility:
      type: string
      enum:
      - public
      - hidden
    position:
      type: integer
    defaultSort:
      type: string
      enum:
      - latest
      - hot
    topicCount:
      type: integer
      format: int64
    commentCount:
      type: integer
      format: int64
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
Tag:
  type: object
  required:
  - id
  - slug
  - name
  - description
  - icon
  - iconColor
  - status
  - topicCount
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
    slug:
      type: string
    name:
      type: string
    description:
      type: string
    icon:
      type: string
      description: Nuxt Icon name such as i-tabler-tag. Empty string means no custom icon.
    iconColor:
      type: string
      description: Hex color such as '#2563eb'. Empty string means use the admin theme accent.
    status:
      "$ref": "#/TagStatus"
    topicCount:
      type: integer
      format: int64
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
TopicTagSummary:
  type: object
  required:
  - id
  - slug
  - name
  - status
  properties:
    id:
      type: integer
      format: int64
    slug:
      type: string
    name:
      type: string
    status:
      "$ref": "#/TagStatus"
ForumSettings:
  type: object
  required:
  - defaultCategorySlug
  - tagCreationMode
  - tagPublicPages
  - tagMinPerTopic
  - tagMaxPerTopic
  - topicsPerPage
  - commentsPerPage
  - topicTitleMinRunes
  - topicTitleMaxRunes
  - topicContentMinRunes
  - topicContentMaxRunes
  - topicEditWindowMinutes
  - topicCooldownSeconds
  - dailyTopicLimit
  - commentMinRunes
  - commentMaxRunes
  - commentMaxNestingDepth
  - treeDescendantsPerRoot
  - commentEditWindowMinutes
  - commentCooldownSeconds
  - dailyCommentLimit
  - excerptRuneLimit
  - guestRead
  - listDefaultSort
  - listHotWindowDays
  - allowAuthorCloseReplies
  - allowAuthorDelete
  - autoLockIdleDays
  - showTopicEditMark
  - duplicateTitlePolicy
  - showCommentEditMark
  - softDeleteVisibility
  - mentionsEnabled
  - mentionsMaxPerPost
  properties:
    defaultCategorySlug:
      type: string
    tagCreationMode:
      "$ref": "#/TagCreationMode"
    tagPublicPages:
      type: boolean
    tagMinPerTopic:
      type: integer
      minimum: 0
      maximum: 10
    tagMaxPerTopic:
      type: integer
      minimum: 0
      maximum: 10
    topicsPerPage:
      type: integer
      default: 20
      minimum: 1
      maximum: 100
    commentsPerPage:
      type: integer
      default: 20
      minimum: 1
      maximum: 100
    topicTitleMinRunes:
      type: integer
      minimum: 1
      maximum: 200
    topicTitleMaxRunes:
      type: integer
      minimum: 1
      maximum: 200
    topicContentMinRunes:
      type: integer
      minimum: 0
      maximum: 200000
    topicContentMaxRunes:
      type: integer
      minimum: 1
      maximum: 200000
    topicEditWindowMinutes:
      type: integer
      minimum: 0
      maximum: 10080
      description: 0 means unlimited author edit window
    topicCooldownSeconds:
      type: integer
      minimum: 0
      maximum: 86400
      description: 0 means no cooldown between topic creates
    dailyTopicLimit:
      type: integer
      minimum: 0
      maximum: 10000
      description: 0 means unlimited daily topics
    commentMinRunes:
      type: integer
      minimum: 0
      maximum: 50000
    commentMaxRunes:
      type: integer
      minimum: 1
      maximum: 50000
    commentMaxNestingDepth:
      type: integer
      minimum: 0
      maximum: 20
    treeDescendantsPerRoot:
      type: integer
      default: 50
      minimum: 1
      maximum: 100
      description: >
        Maximum number of descendant comments loaded under each root on view=tree
        (D2). Overflow sets hasMoreChildren; clients load more via listCommentReplies.
        Restore defaults sets this back to 50. Same default on all clients (no
        mobile-only lower API cap).
    commentEditWindowMinutes:
      type: integer
      minimum: 0
      maximum: 10080
    commentCooldownSeconds:
      type: integer
      minimum: 0
      maximum: 86400
    dailyCommentLimit:
      type: integer
      minimum: 0
      maximum: 10000
    excerptRuneLimit:
      type: integer
      minimum: 40
      maximum: 500
    guestRead:
      type: string
      enum: [public, login_required]
      description: Whether guests may read public forum content
    listDefaultSort:
      type: string
      enum: [latest, active, hot]
    listHotWindowDays:
      type: integer
      minimum: 1
      maximum: 90
    allowAuthorCloseReplies:
      type: boolean
    allowAuthorDelete:
      type: boolean
    autoLockIdleDays:
      type: integer
      minimum: 0
      maximum: 3650
      description: 0 disables auto-lock of idle topics
    showTopicEditMark:
      type: boolean
    duplicateTitlePolicy:
      type: string
      enum: [off, warn, block]
    showCommentEditMark:
      type: boolean
    softDeleteVisibility:
      type: string
      enum: [author_and_staff, staff_only, hidden]
    mentionsEnabled:
      type: boolean
    mentionsMaxPerPost:
      type: integer
      minimum: 0
      maximum: 50
      description: 0 means unlimited mentions when mentions are enabled
ForumUser:
  type: object
  required:
  - id
  - username
  - displayName
  - avatar
  properties:
    id:
      type: integer
      format: int64
    username:
      type: string
    displayName:
      type: string
    avatar:
      "$ref": "../schemas/profile.yaml#/AvatarView"
ForumRevisionList:
  type: object
  required:
  - items
  - perPage
  - hasMore
  properties:
    items:
      type: array
      items:
        "$ref": "#/ForumRevisionSummary"
    perPage:
      type: integer
    hasMore:
      type: boolean
    nextCursor:
      type: string
      description: Opaque keyset cursor for the next page.
ForumRevisionSummary:
  type: object
  description: Revision header. List responses use this schema and never include raw source.
  required:
  - id
  - revisionNo
  - current
  - operation
  - origin
  - changedFields
  - committedAt
  - snapshotComplete
  - restorableFields
  - redacted
  properties:
    id:
      type: integer
      format: int64
    revisionNo:
      type: integer
      format: int64
    current:
      type: boolean
    actor:
      "$ref": "#/ForumUser"
    operation:
      type: string
      enum: [create, edit, restore, migration]
    origin:
      type: string
      enum: [self, staff, migration]
    reason:
      type: string
      description: Staff/self edit reason visible only through authorized history/admin surfaces.
    changedFields:
      type: array
      items:
        type: string
        enum: [title, content, category, tags, attachments]
    committedAt:
      type: string
      format: date-time
    restoredFromRevisionNo:
      type: integer
      format: int64
    snapshotComplete:
      type: boolean
      description: False for legacy body-only rows that cannot honestly restore metadata.
    restorableFields:
      type: array
      items:
        type: string
        enum: [title, content, category, tags, attachments]
    redacted:
      type: boolean
      description: Redacted revisions keep the header but cannot return source or preview.
ForumRevisionDetail:
  allOf:
  - "$ref": "#/ForumRevisionSummary"
  - type: object
    description: Authorized revision detail. This schema is never used by list responses.
    required:
    - rawContent
    - sourceFormat
    - editorType
    - renderVersion
    - contentHash
    - attachments
    properties:
      rawContent:
        type: string
        description: Historical source returned only after the matching history permission check.
      sourceFormat:
        "$ref": "#/SourceFormat"
      editorType:
        type: string
      editorVersion:
        type: string
      renderVersion:
        type: string
      contentHash:
        type: string
      attachments:
        "$ref": "#/AttachmentAvailabilitySummary"
      preview:
        "$ref": "#/HistoricalPreview"
      topicMetadata:
        "$ref": "#/TopicRevisionMetadata"
RestoreRevisionRequest:
  type: object
  required: [expectedRevision, reason]
  properties:
    expectedRevision:
      type: integer
      format: int64
      minimum: 1
    reason:
      type: string
      minLength: 1
      maxLength: 500
      description: Mandatory restore audit reason.
RedactRevisionRequest:
  type: object
  required: [expectedRevision, reason, confirmation]
  properties:
    expectedRevision:
      type: integer
      format: int64
      minimum: 1
    reason:
      type: string
      minLength: 1
      maxLength: 500
    confirmation:
      type: string
      enum: [REDACT]
      description: Typed irreversible-redaction confirmation.
RevisionRedactionResult:
  type: object
  required: [redacted]
  properties:
    redacted:
      type: boolean
HistoricalPreview:
  type: object
  required:
  - htmlContent
  - plainText
  - excerpt
  - renderVersion
  properties:
    htmlContent:
      type: string
      description: Safe historical preview rendered on demand with the current renderer.
    plainText:
      type: string
    excerpt:
      type: string
    renderVersion:
      type: string
AttachmentAvailabilitySummary:
  type: object
  required:
  - ids
  - total
  properties:
    ids:
      type: array
      items:
        type: integer
        format: int64
    total:
      type: integer
TopicRevisionMetadata:
  type: object
  required:
  - tagSlugs
  properties:
    title:
      type: string
    categorySlug:
      type: string
    tagSlugs:
      type: array
      items:
        type: string
AdminForumContentList:
  type: object
  required:
  - items
  - perPage
  - hasMore
  properties:
    items:
      type: array
      items:
        "$ref": "#/AdminForumContentRow"
    perPage:
      type: integer
    hasMore:
      type: boolean
    nextCursor:
      type: string
AdminForumContentRow:
  type: object
  required:
  - targetType
  - id
  - authorUserId
  - status
  - excerpt
  - currentRevision
  - createdAt
  - updatedAt
  properties:
    targetType:
      type: string
      enum: [topic, comment]
    id:
      type: integer
      format: int64
    topicId:
      type: integer
      format: int64
    topicTitle:
      type: string
    categorySlug:
      type: string
    authorUserId:
      type: integer
      format: int64
    author:
      "$ref": "#/ForumUser"
    status:
      type: string
      description: Topic or comment lifecycle status.
    title:
      type: string
    excerpt:
      type: string
    currentRevision:
      type: integer
      format: int64
      minimum: 1
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    tags:
      type: array
      items:
        "$ref": "#/TopicTagSummary"
AdminForumTopicDetail:
  allOf:
  - "$ref": "#/AdminForumContentRow"
  - type: object
    required:
    - content
    - slug
    properties:
      content:
        "$ref": "#/RenderedContent"
      slug:
        type: string
AdminForumCommentDetail:
  allOf:
  - "$ref": "#/AdminForumContentRow"
  - type: object
    required:
    - content
    - rootCommentId
    - pathKey
    - depth
    - replyCount
    properties:
      content:
        "$ref": "#/RenderedContent"
      parentId:
        type:
        - integer
        - 'null'
        format: int64
      rootCommentId:
        type: integer
        format: int64
      pathKey:
        type: string
      depth:
        type: integer
      replyCount:
        type: integer
        format: int64
TopicSummary:
  type: object
  required:
  - id
  - title
  - slug
  - status
  - commentCount
  - viewCount
  - excerpt
  - createdAt
  - updatedAt
  - lastActivityAt
  properties:
    id:
      type: integer
      format: int64
    categoryId:
      type: integer
      format: int64
    categorySlug:
      type: string
    categoryName:
      type: string
    authorUserId:
      type: integer
      format: int64
    author:
      "$ref": "#/ForumUser"
    lastReplyAuthor:
      allOf:
      - "$ref": "#/ForumUser"
      description: Author of the latest active comment; equals author when the topic has no comments. Used by the topic list "last activity" column.
    title:
      type: string
    slug:
      type: string
    status:
      "$ref": "#/TopicStatus"
    isPinned:
      type: boolean
    commentCount:
      type: integer
      format: int64
    viewCount:
      type: integer
      format: int64
    tags:
      type: array
      items:
        "$ref": "#/TopicTagSummary"
    excerpt:
      type: string
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    lastActivityAt:
      type: string
      format: date-time
    edited:
      type: boolean
      description: True only when showTopicEditMark is enabled and the topic has a persisted content revision; clients must not infer this from timestamps.
TopicDetail:
  allOf:
  - "$ref": "#/TopicSummary"
  - type: object
    required:
    - content
    properties:
      content:
        "$ref": "#/RenderedContent"
      extensionActions:
        type: array
        description: Safe host-rendered extension action descriptors from enabled plugins. Executing an action still calls the extension route proxy and its policy checks.
        items:
          "$ref": "#/TopicExtensionAction"
      extensionSidebar:
        type: array
        description: Host-rendered topic sidebar cards/links from forum.topic.sidebar (E2.1). No raw HTML; hostLink or extensionRoute only.
        items:
          "$ref": "#/TopicExtensionSidebarItem"
      extensionBadges:
        type: array
        description: Host-rendered status badges under the topic title from forum.topic.badges (E2.1).
        items:
          "$ref": "#/TopicExtensionBadge"
TopicExtensionAction:
  type: object
  required:
  - extensionId
  - id
  - method
  - url
  properties:
    extensionId:
      type: string
    id:
      type: string
    label:
      type: object
      additionalProperties:
        type: string
    icon:
      type: string
    method:
      type: string
      enum:
      - POST
      - PUT
      - PATCH
      - DELETE
    url:
      type: string
      description: API-relative plugin route path such as /extensions/demo.plugin/topic-actions/bookmark.
    confirm:
      type: boolean
TopicExtensionSidebarItem:
  type: object
  description: Host-rendered topic sidebar card from forum.topic.sidebar (E2.1).
  required:
  - extensionId
  - id
  - order
  - kind
  - url
  properties:
    extensionId:
      type: string
    id:
      type: string
    order:
      type: integer
    label:
      type: object
      additionalProperties:
        type: string
    icon:
      type: string
      description: Icon name from approved set (i-lucide-* or i-tabler-*).
    kind:
      type: string
      enum:
      - extensionRoute
      - hostLink
    method:
      type: string
      description: HTTP method for extensionRoute items.
      enum:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
    url:
      type: string
      description: For extensionRoute, API-relative proxy path; for hostLink, site-relative path (not /api).
TopicExtensionBadge:
  type: object
  description: Host-rendered topic title badge from forum.topic.badges (E2.1).
  required:
  - extensionId
  - id
  - order
  - tone
  properties:
    extensionId:
      type: string
    id:
      type: string
    order:
      type: integer
    label:
      type: object
      additionalProperties:
        type: string
    tone:
      type: string
      enum:
      - neutral
      - info
      - success
      - warning
      - danger
    href:
      type: string
      description: Optional site-relative host path (not /api); omit for display-only badges.
ComposerToolbarAction:
  type: object
  description: Host-rendered composer toolbar action from forum.composer.toolbar (F4.3).
  required:
  - extensionId
  - id
  - order
  - method
  - url
  properties:
    extensionId:
      type: string
    id:
      type: string
    order:
      type: integer
    label:
      type: object
      additionalProperties:
        type: string
    icon:
      type: string
    method:
      type: string
      enum:
      - POST
      - PUT
      - PATCH
      - DELETE
    url:
      type: string
    confirm:
      type: boolean
ApiResponseComposerToolbarActionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ComposerToolbarAction"
TopicList:
  type: object
  required:
  - items
  - total
  - page
  - perPage
  - hasMore
  properties:
    items:
      type: array
      items:
        "$ref": "#/TopicSummary"
    total:
      type: integer
      format: int64
      description: |
        Public list total follows D1 semantics (million-scale read path):
        - single categorySlug → denormalized categories.topic_count (may lag writes briefly; not a request-time COUNT(*))
        - single tagSlug → denormalized tags.topic_count (same)
        - unfiltered home → sum of public category counters (approximate; may be stale)
        - category+tag together → approximate (min of the two counters; not intersection-exact)
        Clients must tolerate stale or approximate totals. Infinite scroll / keyset should prefer hasMore + nextCursor;
        total is secondary and may be approximate on public home.
        Admin/moderation lists may still use exact counts on separate endpoints.
    totalApproximate:
      type: boolean
      description: |
        When true, `total` is an estimate or multi-filter approximation (home / hard-to-count combos).
        When false or omitted, `total` is a denormalized taxonomy counter (category or tag) and may still be briefly stale after writes.
        UI may show an “about N” phrasing only when this flag is true.
    page:
      type: integer
      description: Echo of offset-mode page (1–200). When request used `after`, value is 1 (cursor mode; do not use page for continuation).
    perPage:
      type: integer
    hasMore:
      type: boolean
      description: |
        True when another page of results is available. Required for public list feeds (M5).
        Prefer this over computing from total for infinite scroll.
    nextCursor:
      type: string
      description: |
        Opaque keyset cursor for the next page. Present when hasMore is true; pass as query `after`.
        Bound to the sort used for this response; do not reuse across different sort values.
        Empty or omitted when hasMore is false.
    extensionListBadges:
      type: array
      description: Host-rendered list-row badges from forum.topic.list.badges (E2.4). Returned once per list; clients attach to each row. No per-row plugin RPC.
      items:
        "$ref": "#/TopicExtensionBadge"
# 搜索结果与首页 TopicList 行同构，便于复用列表组件（头像 / 最近回复 / 时间戳）。
SearchOutput:
  type: object
  required:
  - items
  - total
  - page
  - perPage
  properties:
    items:
      type: array
      items:
        "$ref": "#/TopicSummary"
    total:
      type: integer
      format: int64
    page:
      type: integer
    perPage:
      type: integer
ReindexRun:
  type: object
  required:
  - id
  - total
  - status
  - startedAt
  properties:
    id:
      type: integer
      format: int64
    total:
      type: integer
      format: int64
    status:
      type: string
      enum: [running, completed, failed]
    startedAt:
      type: string
      format: date-time
    finishedAt:
      type: string
      format: date-time
      nullable: true
    startedByUserId:
      type: integer
      format: int64
      nullable: true
    error:
      type: string
ReindexStatus:
  allOf:
  - "$ref": "#/ReindexRun"
  - type: object
    required: [processed, remaining, percent]
    properties:
      processed:
        type: integer
        format: int64
      remaining:
        type: integer
        format: int64
      percent:
        type: integer
        minimum: 0
        maximum: 100
ApiResponseReindexRun:
  type: object
  required: [code, message, data]
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ReindexRun"
ApiResponseReindexStatus:
  type: object
  required: [code, message, data]
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ReindexStatus"
ApiResponseReindexRunList:
  type: object
  required: [code, message, data]
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ReindexRun"
SearchProviderItem:
  type: object
  required: [extensionId, label, healthy]
  properties:
    extensionId:
      type: string
    label:
      type: string
    healthy:
      type: boolean
    isDefault:
      type: boolean
SearchProvidersState:
  type: object
  required: [items, selected, pinned, defaultExtensionId]
  properties:
    items:
      type: array
      items:
        "$ref": "#/SearchProviderItem"
    selected:
      "$ref": "#/SearchProviderItem"
    pinned:
      type: boolean
      description: Whether an operator explicit pin exists (false means resolved default site search).
    defaultExtensionId:
      type: string
      description: Protected built-in site search extension id (sforum.search-site).
SearchProviderSelectionRequest:
  type: object
  required: [extensionId]
  properties:
    extensionId:
      type: string
ApiResponseSearchProvidersState:
  type: object
  required: [code, message, data]
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/SearchProvidersState"
Comment:
  type: object
  required:
  - id
  - topicId
  - authorUserId
  - rootCommentId
  - pathKey
  - depth
  - replyCount
  - status
  - content
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
    topicId:
      type: integer
      format: int64
    authorUserId:
      type: integer
      format: int64
    author:
      "$ref": "#/ForumUser"
    parentId:
      type:
      - integer
      - 'null'
      format: int64
    rootCommentId:
      type: integer
      format: int64
    pathKey:
      type: string
    depth:
      type: integer
    replyCount:
      type: integer
      format: int64
    status:
      "$ref": "#/CommentStatus"
    content:
      "$ref": "#/RenderedContent"
    replyTo:
      "$ref": "#/ReplyReference"
    children:
      type: array
      items:
        "$ref": "#/Comment"
    hasMoreChildren:
      type: boolean
      description: >
        When true on a tree root (or node), descendant payload was truncated by
        treeDescendantsPerRoot. Clients should offer "load more replies" via
        GET /comments/{commentID}/replies and merge results.
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    edited:
      type: boolean
      description: True only when showCommentEditMark is enabled and the comment has a persisted content revision; clients must not infer this from timestamps.
ReplyReference:
  type: object
  required:
  - id
  - excerpt
  - depth
  properties:
    id:
      type: integer
      format: int64
    author:
      "$ref": "#/ForumUser"
    excerpt:
      type: string
    depth:
      type: integer
CommentList:
  type: object
  required:
  - items
  - total
  - page
  - perPage
  - view
  - hasMore
  properties:
    items:
      type: array
      items:
        "$ref": "#/Comment"
    total:
      type: integer
      format: int64
      description: >
        flat: topic comment total (public path prefers denormalized topics.comment_count;
        may be briefly stale after writes). tree: root comment count for the topic.
        Paths that include soft-deleted rows may use exact COUNT instead.
        Prefer hasMore + nextCursor for deep flat scroll (M5).
    page:
      type: integer
      description: Offset-mode page echo. Cursor mode (`after`) sets page to 1.
    perPage:
      type: integer
    view:
      type: string
      enum:
      - tree
      - flat
    hasMore:
      type: boolean
      description: True when another page is available (flat keyset or tree root page).
    nextCursor:
      type: string
      description: |
        Opaque forward cursor for view=flat (path_key + id). Pass as query `after`.
        Omitted/empty when hasMore is false. Tree view may omit nextCursor (use page).
    extensionActions:
      type: array
      description: Host-rendered comment row actions from forum.comment.actions (E2.2). Listed once per page; clients attach them to each row menu. Unsafe calls still go through the extension route proxy.
      items:
        "$ref": "#/CommentExtensionAction"
CommentExtensionAction:
  type: object
  description: Host-rendered comment row action from forum.comment.actions (E2.2).
  required:
  - extensionId
  - id
  - method
  - url
  properties:
    extensionId:
      type: string
    id:
      type: string
    label:
      type: object
      additionalProperties:
        type: string
    icon:
      type: string
    method:
      type: string
      enum:
      - POST
      - PUT
      - PATCH
      - DELETE
    url:
      type: string
      description: API-relative plugin route path such as /extensions/demo.plugin/comment-actions/flag.
    confirm:
      type: boolean
    requiresAuth:
      type: boolean
      description: UX hint only — hide for guests when true; host still enforces login on unsafe extension routes.
CreateTopicRequest:
  type: object
  required:
  - title
  - content
  properties:
    categorySlug:
      type: string
      default: general
    tagSlugs:
      type: array
      maxItems: 10
      items:
        type: string
    title:
      type: string
    content:
      "$ref": "#/ContentInput"
CreateCommentRequest:
  type: object
  required:
  - content
  properties:
    parentId:
      type:
      - integer
      - 'null'
      format: int64
    content:
      "$ref": "#/ContentInput"
UpdateCommentRequest:
  type: object
  required:
  - content
  - expectedRevision
  properties:
    expectedRevision:
      type: integer
      format: int64
      minimum: 1
    reason:
      type: string
      maxLength: 500
    content:
      "$ref": "#/ContentInput"
UpdateTopicRequest:
  type: object
  description: |
    `expectedRevision` is mandatory. Other fields are optional and omitted fields are left unchanged.
    An explicit empty `tagSlugs` array clears tags; an absent key leaves tags unchanged.
  required:
  - expectedRevision
  properties:
    expectedRevision:
      type: integer
      format: int64
      minimum: 1
    reason:
      type: string
      maxLength: 500
    categorySlug:
      type: string
    title:
      type: string
    tagSlugs:
      type: array
      maxItems: 10
      items:
        type: string
    content:
      "$ref": "#/ContentInput"
TopicAction:
  type: object
  required:
  - topicId
  - status
  - isPinned
  properties:
    topicId:
      type: integer
      format: int64
    status:
      "$ref": "#/TopicStatus"
    isPinned:
      type: boolean
ApiResponseTopicAction:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/TopicAction"
CreateCategoryGroupRequest:
  type: object
  required:
  - slug
  - name
  properties:
    slug:
      type: string
    name:
      type: string
    description:
      type: string
      default: ''
    visibility:
      type: string
      enum:
      - public
      - hidden
      default: public
    position:
      type: integer
      default: 0
UpdateCategoryGroupRequest:
  type: object
  properties:
    slug:
      type: string
    name:
      type: string
    description:
      type: string
    visibility:
      type: string
      enum:
      - public
      - hidden
    position:
      type: integer
CreateCategoryRequest:
  type: object
  required:
  - groupId
  - slug
  - name
  properties:
    groupId:
      type: integer
      format: int64
    slug:
      type: string
    name:
      type: string
    description:
      type: string
      default: ''
    icon:
      type: string
      default: ''
    iconColor:
      type: string
      default: ''
    visibility:
      type: string
      enum:
      - public
      - hidden
      default: public
    position:
      type: integer
      default: 0
    defaultSort:
      type: string
      enum:
      - latest
      - hot
      default: latest
UpdateCategoryRequest:
  type: object
  properties:
    groupId:
      type: integer
      format: int64
    slug:
      type: string
    name:
      type: string
    description:
      type: string
    icon:
      type: string
    iconColor:
      type: string
    visibility:
      type: string
      enum:
      - public
      - hidden
    position:
      type: integer
    defaultSort:
      type: string
      enum:
      - latest
      - hot
CreateTagRequest:
  type: object
  required:
  - slug
  - name
  properties:
    slug:
      type: string
    name:
      type: string
    description:
      type: string
      default: ''
    icon:
      type: string
      default: ''
    iconColor:
      type: string
      default: ''
    status:
      "$ref": "#/TagStatus"
      default: active
UpdateTagRequest:
  type: object
  properties:
    slug:
      type: string
    name:
      type: string
    description:
      type: string
    icon:
      type: string
    iconColor:
      type: string
    status:
      "$ref": "#/TagStatus"
UpdateForumSettingsRequest:
  type: object
  properties:
    defaultCategorySlug:
      type: string
    tagCreationMode:
      "$ref": "#/TagCreationMode"
    tagPublicPages:
      type: boolean
    tagMinPerTopic:
      type: integer
      minimum: 0
      maximum: 10
    tagMaxPerTopic:
      type: integer
      minimum: 0
      maximum: 10
    topicsPerPage:
      type: integer
      default: 20
      minimum: 1
      maximum: 100
    commentsPerPage:
      type: integer
      default: 20
      minimum: 1
      maximum: 100
    topicTitleMinRunes:
      type: integer
      minimum: 1
      maximum: 200
    topicTitleMaxRunes:
      type: integer
      minimum: 1
      maximum: 200
    topicContentMinRunes:
      type: integer
      minimum: 0
      maximum: 200000
    topicContentMaxRunes:
      type: integer
      minimum: 1
      maximum: 200000
    topicEditWindowMinutes:
      type: integer
      minimum: 0
      maximum: 10080
    topicCooldownSeconds:
      type: integer
      minimum: 0
      maximum: 86400
    dailyTopicLimit:
      type: integer
      minimum: 0
      maximum: 10000
    commentMinRunes:
      type: integer
      minimum: 0
      maximum: 50000
    commentMaxRunes:
      type: integer
      minimum: 1
      maximum: 50000
    commentMaxNestingDepth:
      type: integer
      minimum: 0
      maximum: 20
    treeDescendantsPerRoot:
      type: integer
      default: 50
      minimum: 1
      maximum: 100
      description: Maximum descendants per root on view=tree (D2).
    commentEditWindowMinutes:
      type: integer
      minimum: 0
      maximum: 10080
    commentCooldownSeconds:
      type: integer
      minimum: 0
      maximum: 86400
    dailyCommentLimit:
      type: integer
      minimum: 0
      maximum: 10000
    excerptRuneLimit:
      type: integer
      minimum: 40
      maximum: 500
    guestRead:
      type: string
      enum: [public, login_required]
    listDefaultSort:
      type: string
      enum: [latest, active, hot]
    listHotWindowDays:
      type: integer
      minimum: 1
      maximum: 90
    allowAuthorCloseReplies:
      type: boolean
    allowAuthorDelete:
      type: boolean
    autoLockIdleDays:
      type: integer
      minimum: 0
      maximum: 3650
    showTopicEditMark:
      type: boolean
    duplicateTitlePolicy:
      type: string
      enum: [off, warn, block]
    showCommentEditMark:
      type: boolean
    softDeleteVisibility:
      type: string
      enum: [author_and_staff, staff_only, hidden]
    mentionsEnabled:
      type: boolean
    mentionsMaxPerPost:
      type: integer
      minimum: 0
      maximum: 50
AuthorReviewItem:
  type: object
  required: [targetType, targetId, title, excerpt, status, reviewNote, createdAt]
  properties:
    targetType: {type: string, enum: [topic, comment]}
    targetId: {type: integer, format: int64}
    topicId: {type: integer, format: int64}
    title: {type: string}
    excerpt: {type: string}
    status: {type: string, enum: [pending, rejected]}
    reviewNote: {type: string}
    createdAt: {type: string, format: date-time}
AuthorReviewList:
  type: object
  required: [items]
  properties:
    items: {type: array, items: {$ref: "#/AuthorReviewItem"}}
ApiResponseAuthorReviewList:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/AuthorReviewList"}
