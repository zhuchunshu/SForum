ApiResponseCurrentUser:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/CurrentUser"
ApiResponseRegistrationStatus:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/RegistrationStatus"
ApiResponseRole:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/Role"
ApiResponseRoleList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/Role"
RoleSuggestion:
  type: object
  allOf:
  - if:
      properties:
        approvalState:
          enum:
          - pending
          - rejected
      required:
      - approvalState
    then:
      properties:
        applied:
          const: false
  required:
  - id
  - permissionKey
  - ownerExtensionId
  - extensionVersionId
  - extensionVersion
  - packageDigest
  - permissionContractVersion
  - declarationDigest
  - roleKey
  - approvalState
  - applied
  - revision
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
      minimum: 1
    permissionKey:
      type: string
    ownerExtensionId:
      type: string
    extensionVersionId:
      type: integer
      format: int64
      minimum: 1
    extensionVersion:
      type: string
    packageDigest:
      type: string
      pattern: '^[a-f0-9]{64}$'
    permissionContractVersion:
      type: string
    declarationDigest:
      type: string
      pattern: '^[a-f0-9]{64}$'
    roleKey:
      type: string
    approvalState:
      type: string
      enum:
      - pending
      - approved
      - rejected
    applied:
      type: boolean
      description: True only when immutable Host grant evidence exists.
    revision:
      type: integer
      format: int64
      minimum: 1
    decidedByUserId:
      type: integer
      format: int64
      minimum: 1
    decisionAuditEventId:
      type: integer
      format: int64
      minimum: 1
    decidedAt:
      type: string
      format: date-time
    appliedByUserId:
      type: integer
      format: int64
      minimum: 1
    appliedAuditEventId:
      type: integer
      format: int64
      minimum: 1
    appliedAt:
      type: string
      format: date-time
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
RoleSuggestionPage:
  type: object
  required:
  - items
  properties:
    items:
      type: array
      maxItems: 100
      items:
        "$ref": "#/RoleSuggestion"
    nextCursor:
      type: string
RoleSuggestionDecisionInput:
  type: object
  additionalProperties: false
  required:
  - expectedRevision
  - approvalState
  properties:
    expectedRevision:
      type: integer
      format: int64
      minimum: 1
    approvalState:
      type: string
      enum:
      - approved
      - rejected
ApiResponseRoleSuggestion:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/RoleSuggestion"
ApiResponseRoleSuggestionPage:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/RoleSuggestionPage"
ApiResponsePermissionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/Permission"
ApiResponsePermissionMatrix:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/PermissionMatrix"
ApiResponseAdminUserList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/AdminUserList"
ApiResponseAdminUserDetail:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/AdminUserDetail"
ApiResponseAltchaChallenge:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/AltchaChallenge"
CurrentUser:
  type: object
  required:
  - id
  - username
  - displayName
  - locale
  - status
  - isInitialSuperAdmin
  - avatar
  - roleKeys
  - permissions
  properties:
    id:
      type: integer
      format: int64
    username:
      type: string
    displayName:
      type: string
    locale:
      type: string
    status:
      type: string
      enum:
      - active
      - disabled
      - banned
    isInitialSuperAdmin:
      type: boolean
    avatar:
      "$ref": "../schemas/profile.yaml#/AvatarView"
    roleKeys:
      type: array
      items:
        type: string
    permissions:
      type: array
      items:
        type: string
RegistrationStatus:
  type: object
  required:
  - nextUserIsInitialSuperAdmin
  - registrationEnabled
  properties:
    nextUserIsInitialSuperAdmin:
      type: boolean
      description: Always false on the public endpoint to avoid leaking the bootstrap window.
    registrationEnabled:
      type: boolean
      description: Whether the UI should offer registration. Forced true when no users exist yet (bootstrap); otherwise follows identity.registration.enabled.
RegisterRequest:
  type: object
  required:
  - username
  - email
  - password
  properties:
    username:
      type: string
      minLength: 1
      description: Trimmed username. Must not be blank.
    email:
      type: string
      format: email
      description: Trimmed email address. Must not be blank.
    password:
      type: string
      format: password
      minLength: 12
      description: Must contain at least 12 characters.
    displayName:
      type: string
      description: Optional display name. Blank values default to username.
    locale:
      type: string
    humanVerification:
      "$ref": "#/HumanVerification"
      description: Required when the deployment enables human verification for registration.
HumanVerification:
  type: object
  required:
  - provider
  - token
  properties:
    provider:
      type: string
      enum:
      - altcha
    token:
      type: string
      description: Base64-encoded ALTCHA payload emitted by the widget.
AltchaChallenge:
  type: object
  required:
  - parameters
  properties:
    parameters:
      type: object
      required:
      - algorithm
      - nonce
      - salt
      - cost
      - keyLength
      - keyPrefix
      properties:
        algorithm:
          type: string
          examples:
          - PBKDF2/SHA-256
        nonce:
          type: string
        salt:
          type: string
        cost:
          type: integer
        keyLength:
          type: integer
        keyPrefix:
          type: string
        keySignature:
          type: string
        memoryCost:
          type: integer
        parallelism:
          type: integer
        expiresAt:
          type: integer
          format: int64
        data:
          type: object
          additionalProperties: true
    signature:
      type: string
LoginRequest:
  type: object
  required:
  - login
  - password
  properties:
    login:
      type: string
      description: Username or email address.
    password:
      type: string
      format: password
    humanVerification:
      "$ref": "#/HumanVerification"
      description: Required only after the account-level risk threshold is reached and the password is correct.
Role:
  type: object
  required:
  - id
  - key
  - alias
  - description
  - isSystem
  - isDefault
  - isDeletable
  - isEnabled
  - permissionKeys
  properties:
    id:
      type: integer
      format: int64
    key:
      type: string
    alias:
      type: string
    description:
      type: string
    isSystem:
      type: boolean
    isDefault:
      type: boolean
    isDeletable:
      type: boolean
    isEnabled:
      type: boolean
    permissionKeys:
      type: array
      items:
        type: string
RoleCreateInput:
  type: object
  required:
  - key
  - alias
  properties:
    key:
      type: string
      minLength: 1
      pattern: "^[a-z0-9_.-]+$"
      description: Stable role key used in URLs. Trimmed value may contain lowercase letters, numbers, dots, hyphens, or underscores.
    alias:
      type: string
      minLength: 1
      description: Display name shown to administrators and users. Trimmed value must not be blank.
    description:
      type: string
      description: Optional role description.
RoleUpdateInput:
  type: object
  required:
  - alias
  properties:
    alias:
      type: string
      minLength: 1
      description: Display name shown to administrators and users. Trimmed value must not be blank.
    description:
      type: string
      description: Optional role description.
Permission:
  type: object
  required:
  - key
  - module
  - label
  - description
  properties:
    key:
      type: string
    module:
      type: string
    label:
      type: string
      description: Locale-resolved display label supplied by an extension, or empty for Host permissions that use the built-in UI catalog.
    description:
      type: string
      description: Locale-resolved extension description or the Host permission fallback description.
RolePermissionSet:
  type: object
  required:
  - roleKey
  - permissionKeys
  properties:
    roleKey:
      type: string
    permissionKeys:
      type: array
      items:
        type: string
PermissionMatrix:
  type: object
  required:
  - permissions
  - roles
  properties:
    permissions:
      type: array
      items:
        "$ref": "#/Permission"
    roles:
      type: array
      items:
        "$ref": "#/RolePermissionSet"
PermissionOverrides:
  type: object
  required:
  - allow
  - deny
  properties:
    allow:
      type: array
      items:
        type: string
    deny:
      type: array
      items:
        type: string
AdminUserSummary:
  type: object
  required:
  - id
  - username
  - email
  - displayName
  - locale
  - status
  - isInitialSuperAdmin
  - roleKeys
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
    username:
      type: string
    email:
      type: string
      format: email
    displayName:
      type: string
    locale:
      type: string
    status:
      type: string
      enum:
      - active
      - disabled
      - banned
    isInitialSuperAdmin:
      type: boolean
    roleKeys:
      type: array
      items:
        type: string
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
AdminUserProfile:
  type: object
  required:
  - bio
  - signature
  - location
  - websiteUrl
  properties:
    bio:
      type: string
    signature:
      type: string
    location:
      type: string
    websiteUrl:
      type: string
AdminUserDetail:
  allOf:
  - "$ref": "#/AdminUserSummary"
  - type: object
    required:
    - permissions
    - permissionOverrides
    - profile
    - activity
    - sessions
    - recentAuthEvents
    properties:
      permissions:
        type: array
        items:
          type: string
      permissionOverrides:
        "$ref": "#/PermissionOverrides"
      profile:
        "$ref": "#/AdminUserProfile"
      activity:
        "$ref": "#/AdminUserActivity"
        description: Activity summary for admin inspection (topic/comment counts, last login IP/UA).
      sessions:
        type: array
        description: Recent login sessions with full client IP and raw User-Agent (admin-only).
        items:
          "$ref": "#/AdminSessionInspect"
      recentAuthEvents:
        type: array
        description: Recent login/register audit events with IP and User-Agent.
        items:
          "$ref": "#/AdminAuthEvent"
      passwordChangedAt:
        type: string
        format: date-time
        nullable: true
        description: Last password change time when a password credential exists.
AdminUserActivity:
  type: object
  required:
  - topicCount
  - commentCount
  - activeSessionCount
  - totalSessionCount
  - lastLoginIP
  - lastLoginUserAgent
  properties:
    topicCount:
      type: integer
      format: int64
    commentCount:
      type: integer
      format: int64
    activeSessionCount:
      type: integer
      format: int64
    totalSessionCount:
      type: integer
      format: int64
    lastLoginAt:
      type: string
      format: date-time
      nullable: true
    lastLoginIP:
      type: string
      description: Full client IP from the latest login audit or session (admin-only).
    lastLoginUserAgent:
      type: string
      description: Raw User-Agent from the latest login audit or session (admin-only).
    lastSeenAt:
      type: string
      format: date-time
      nullable: true
AdminSessionInspect:
  type: object
  required:
  - id
  - deviceName
  - browser
  - os
  - ipPrefix
  - ipAddress
  - userAgent
  - createdAt
  - lastSeenAt
  - isActive
  - revokeReason
  properties:
    id:
      type: string
      description: Opaque session id (not an auth credential).
    deviceName:
      type: string
    browser:
      type: string
    os:
      type: string
    ipPrefix:
      type: string
      description: Masked IP prefix used in self-service device lists.
    ipAddress:
      type: string
      description: Full client IP for admin inspection. Empty after privacy clear.
    userAgent:
      type: string
      description: Raw User-Agent string.
    createdAt:
      type: string
      format: date-time
    lastSeenAt:
      type: string
      format: date-time
    isActive:
      type: boolean
    revokedAt:
      type: string
      format: date-time
      nullable: true
    revokeReason:
      type: string
AdminAuthEvent:
  type: object
  required:
  - id
  - action
  - ipAddress
  - userAgent
  - sessionHash
  - createdAt
  properties:
    id:
      type: integer
      format: int64
    action:
      type: string
      description: Stable audit action, e.g. auth.login.success or auth.register.success.
    ipAddress:
      type: string
    userAgent:
      type: string
    sessionHash:
      type: string
      description: HMAC of the session id for audit correlation; not usable as a credential.
    createdAt:
      type: string
      format: date-time
AdminUserList:
  type: object
  required:
  - items
  - total
  - page
  - perPage
  properties:
    items:
      type: array
      items:
        "$ref": "#/AdminUserSummary"
    total:
      type: integer
      format: int64
    page:
      type: integer
    perPage:
      type: integer
AdminUpdateUserInput:
  type: object
  description: |
    Partial admin update for a user account and public profile.
    Omitted fields are left unchanged. Requires `user.manage`;
    setting status to `banned` also requires `user.ban`.
  properties:
    username:
      type: string
    email:
      type: string
      format: email
    displayName:
      type: string
    locale:
      type: string
      enum:
      - zh-CN
      - en-US
    status:
      type: string
      enum:
      - active
      - disabled
      - banned
    bio:
      type: string
      maxLength: 500
    signature:
      type: string
      maxLength: 200
    location:
      type: string
      maxLength: 100
    websiteUrl:
      type: string
      maxLength: 200
UserRolesInput:
  type: object
  required:
  - roleKeys
  properties:
    roleKeys:
      type: array
      items:
        type: string
UserPermissionOverridesInput:
  "$ref": "#/PermissionOverrides"
PasswordResetRequest:
  type: object
  required:
  - email
  properties:
    email:
      type: string
      format: email
    humanVerification:
      "$ref": "#/HumanVerification"
PasswordResetConfirmRequest:
  type: object
  required:
  - token
  - newPassword
  properties:
    token:
      type: string
    newPassword:
      type: string
      minLength: 12
ApiResponsePasswordResetSent:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: object
      required:
      - sent
      properties:
        sent:
          type: boolean
ApiResponsePasswordResetConfirmed:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: object
      required:
      - reset
      properties:
        reset:
          type: boolean

# 账号安全 / 登录设备管理。opaque 会话标识，非认证凭证，仅用于指定下线哪一条。
LoginSession:
  type: object
  required:
  - id
  - deviceName
  - browser
  - os
  - ipPrefix
  - createdAt
  - lastSeenAt
  - isCurrent
  properties:
    id:
      type: string
      description: Opaque session id (not an auth credential). Used to target a device for revocation.
    deviceName:
      type: string
      description: Display name parsed from User-Agent, e.g. "Chrome on macOS".
    browser:
      type: string
    os:
      type: string
    ipPrefix:
      type: string
      description: Masked IP prefix, e.g. "1.2.3.*".
    createdAt:
      type: string
      format: date-time
    lastSeenAt:
      type: string
      format: date-time
    isCurrent:
      type: boolean
      description: Whether this is the device making the current request.
    revokedAt:
      type: string
      format: date-time
      nullable: true
      description: Present (in history view) when the session has been revoked.
    revokeReason:
      type: string
      description: Why the session was revoked (logout / revoke_device / revoke_others / max_exceeded / password_reset / session_replaced).
LoginSessionList:
  type: object
  required:
  - items
  - total
  - page
  - perPage
  properties:
    items:
      type: array
      items:
        "$ref": "#/LoginSession"
    total:
      type: integer
    page:
      type: integer
    perPage:
      type: integer
ApiResponseLoginSessionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/LoginSessionList"
ApiResponseRevokeOthers:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: object
      required:
      - revoked
      properties:
        revoked:
          type: integer
          description: Number of other sessions that were revoked.
ApiResponseAdminSetUserPassword:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: object
      required:
      - revokedSessions
      properties:
        revokedSessions:
          type: integer
          description: Number of active sessions revoked after the password change.
          minimum: 0
APIToken:
  type: object
  required: [id, publicId, name, scopes, prefix, createdAt]
  properties:
    id: { type: integer, format: int64 }
    publicId: { type: string }
    name: { type: string }
    scopes: { type: array, items: { type: string } }
    prefix: { type: string }
    lastUsedAt: { type: string, format: date-time }
    expiresAt: { type: string, format: date-time }
    revokedAt: { type: string, format: date-time }
    createdAt: { type: string, format: date-time }
CreatedAPIToken:
  allOf:
  - "$ref": "#/APIToken"
  - type: object
    required: [token]
    properties:
      token:
        type: string
        description: One-time plaintext secret (sft_<publicId>.<secret>).
CreateAPITokenRequest:
  type: object
  required: [name, scopes]
  properties:
    name: { type: string, maxLength: 80 }
    scopes: { type: array, items: { type: string }, minItems: 1 }
    expiresAt: { type: string, format: date-time }
ApiResponseAPITokenList:
  type: object
  required: [code, message, data]
  properties:
    code: { type: integer }
    message: { type: string }
    data:
      type: object
      required: [items]
      properties:
        items: { type: array, items: { "$ref": "#/APIToken" } }
ApiResponseCreatedAPIToken:
  type: object
  required: [code, message, data]
  properties:
    code: { type: integer }
    message: { type: string }
    data: { "$ref": "#/CreatedAPIToken" }

ClearUserClientIPsResult:
  type: object
  required: [sessionsCleared, topicsCleared, commentsCleared]
  properties:
    sessionsCleared:
      type: integer
    topicsCleared:
      type: integer
    commentsCleared:
      type: integer
ApiResponseClearUserClientIPs:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/ClearUserClientIPsResult"}

AuthProviderListItem:
  type: object
  required: [id, kind, contractVersion, operations, activatedOperations]
  properties:
    id: {type: string}
    kind: {type: string, enum: [auth, recovery]}
    contractVersion: {type: string}
    priority: {type: integer}
    operations:
      type: array
      items: {type: string}
    activatedOperations:
      type: array
      description: Host-activated public operations (login, registration, link). Empty when not Host-activated.
      items: {type: string, enum: [login, registration, link]}
    ownerExtensionId:
      type: string
      description: Owner extension id for display only; not an ownership claim.
    label:
      type: string
      description: |
        Localized display name declared by the owning plugin Identity provider
        contribution (labelLocales/label). Host resolves Accept-Language; Core
        does not hard-code vendor brands.
    icon:
      type: string
      description: |
        Optional Iconify/Nuxt Icon name declared by the plugin (for example
        i-tabler-brand-github). Empty means Host uses a generic key icon.

ApiResponseAuthProviderList:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data:
      type: array
      items: {$ref: "#/AuthProviderListItem"}

AuthProviderStartRequest:
  type: object
  required: [correlationId]
  properties:
    correlationId: {type: string, maxLength: 200}
    deviceFingerprint: {type: string}
    clientClass: {type: string}
    redirectHint:
      type: string
      description: Optional safe local return path. Host validates before store/plugin.
    accountHint: {type: string}

AuthProviderStartResult:
  type: object
  required: [providerId, operation, status, correlationId]
  properties:
    providerId: {type: string}
    operation: {type: string}
    status: {type: string, enum: [continue, redirect, challenge]}
    correlationId: {type: string}
    continueToken: {type: string}
    redirectUrl: {type: string}
    challengeKind: {type: string}

ApiResponseAuthProviderStart:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/AuthProviderStartResult"}

AuthProviderCompleteRequest:
  type: object
  required: [correlationId, completionToken]
  properties:
    correlationId: {type: string, maxLength: 200}
    completionToken: {type: string, maxLength: 4000}
    deviceFingerprint: {type: string}
    clientClass: {type: string}
    idempotencyKey: {type: string}

AuthProviderCompleteResult:
  type: object
  required: [providerId, operation]
  description: |
    Public recovery complete shape. Auth login/registration/link complete is
    closed on the public POST path; Host callback never returns raw subject or
    subject digest to the browser.
  properties:
    providerId: {type: string}
    operation: {type: string}
    providerSubjectDigest:
      type: string
      description: Recovery-only subject digest when applicable; auth callbacks never expose this.
    displayName: {type: string}
    emailHint: {type: string}
    userHintId: {type: integer, format: int64}
    linkId: {type: integer, format: int64}
    linkStatus: {type: string}

ApiResponseAuthProviderComplete:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/AuthProviderCompleteResult"}

ExternalRegistrationRequest:
  type: object
  required: [ticket, username, email]
  properties:
    ticket:
      type: string
      description: Host-generated opaque one-time registration ticket.
    username: {type: string}
    email: {type: string, format: email}
    displayName: {type: string}
    locale: {type: string}
    humanVerification:
      type: object
      properties:
        provider: {type: string}
        token: {type: string}

ExternalIdentityItem:
  type: object
  required: [linkId, providerId, status]
  properties:
    linkId: {type: integer, format: int64}
    providerId: {type: string}
    status: {type: string, enum: [active, inert, erased]}
    linkedAt:
      type: string
      format: date-time
    ownerExtensionId: {type: string}

ApiResponseExternalIdentityList:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data:
      type: array
      items: {$ref: "#/ExternalIdentityItem"}

ExternalIdentityUnlinkRequest:
  type: object
  properties:
    expectedRevision:
      type: integer
      format: int64
      description: Optional optimistic revision; defaults to the loaded active link revision.
    requestId:
      type: string
      description: Optional non-IP request id for idempotency; Host generates when omitted.

SetupPasswordRequest:
  type: object
  required: [password]
  properties:
    password:
      type: string
      format: password
      minLength: 1

AdminIdentityProviderItem:
  type: object
  required:
  - id
  - kind
  - discovered
  - trusted
  - enabled
  - configured
  - activated
  - publiclyActivated
  - loginEnabled
  - registrationEnabled
  - linkEnabled
  - revision
  - callbackPath
  properties:
    id: {type: string}
    kind: {type: string, enum: [auth, recovery]}
    contractVersion: {type: string}
    priority: {type: integer}
    operations:
      type: array
      items: {type: string}
    ownerExtensionId: {type: string}
    ownerExtensionVersion: {type: string}
    ownerPackageDigest:
      type: string
      description: Live package digest for operator inspection; never a browser ownership claim.
    discovered:
      type: boolean
      description: >
        Visible in the Host extension/package catalog and/or live Identity Registry
        (including untrusted, disabled, or pre-enable staged built-ins).
    trusted:
      type: boolean
      description: >
        Exact-artifact executable trust is granted, or the live Registry carries
        package digest + version id. Trust/enable authority remains extension lifecycle.
    enabled:
      type: boolean
      description: Live RuntimeInstanceID is present (executable process).
    configured:
      type: boolean
      description: Required extension settings (including secrets) are present.
    probed:
      type: boolean
      description: Last real provider.probe reported ok=true; probe_pending/unavailable must never be treated as success.
    label:
      type: string
      description: >
        Locale-resolved display label from the plugin Identity declaration (Host catalog).
        Core must not invent vendor brands from provider or extension ids.
    icon:
      type: string
      description: >
        Iconify/Nuxt Icon name from the plugin Identity declaration (for example
        i-tabler-brand-github). Empty means the admin UI uses a generic key icon.
    artifactBound: {type: boolean}
    runtimeInstanceId: {type: string}
    activated:
      type: boolean
      description: Host activation intent — at least one of login/registration/link is on.
    publiclyActivated:
      type: boolean
      description: Effective public availability — intent on, artifact bound, enabled, configured, and not Safe Mode.
    loginEnabled: {type: boolean}
    registrationEnabled: {type: boolean}
    linkEnabled: {type: boolean}
    revision: {type: integer, format: int64}
    callbackPath:
      type: string
      description: Public same-origin callback path bridged by the Web Host to the reserved Core callback.
    callbackUrl:
      type: string
      description: Absolute callback URL derived from runtime site.url, with trusted APP_URL fallback (never request Host).
    settingsPath:
      type: string
      description: Admin path for Host-rendered extension settings (SFExtensionSettingsRenderer).
    safeMode: {type: boolean}
    lastProbeAt:
      type: string
      format: date-time
    lastProbeOk: {type: boolean}
    lastProbeReason: {type: string}

ApiResponseAdminIdentityProviderList:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data:
      type: array
      items: {$ref: "#/AdminIdentityProviderItem"}

AdminIdentityProviderPatchRequest:
  type: object
  required: [expectedRevision]
  properties:
    expectedRevision: {type: integer, format: int64}
    loginEnabled: {type: boolean}
    registrationEnabled: {type: boolean}
    linkEnabled: {type: boolean}
    priority: {type: integer}
    ownerExtensionId:
      type: string
      description: Must be omitted or empty; browser ownership claims are rejected.
    ownerPackageDigest:
      type: string
      description: Must be omitted or empty; browser artifact claims are rejected.

ApiResponseAdminIdentityProvider:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/AdminIdentityProviderItem"}

AdminIdentityProviderProbeResult:
  type: object
  required: [providerId, ok, status]
  properties:
    providerId: {type: string}
    ok: {type: boolean}
    status:
      type: string
      description: Stable redacted reason code from the real provider.probe runtime operation (or probe_unavailable).
    reason:
      type: string
      description: Same as status; never contains secrets, tokens, or raw subjects.
    message:
      type: string
      description: Optional redacted human-readable detail; must not claim Client Secret correctness.

ApiResponseAdminIdentityProviderProbe:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/AdminIdentityProviderProbeResult"}

AdminIdentityProvidersResetResult:
  type: object
  required: [resetCount, secretsPreserved]
  properties:
    resetCount: {type: integer}
    secretsPreserved:
      type: boolean
      description: Always true for V1; secrets are never cleared by restore defaults.

ApiResponseAdminIdentityProvidersReset:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/AdminIdentityProvidersResetResult"}

ProfileSection:
  type: object
  required: [providerId, sectionId]
  properties:
    providerId: {type: string}
    sectionId: {type: string}
    title: {type: string}
    priority: {type: integer}
    fields:
      type: object
      additionalProperties: true

ProfileSectionUpdateRequest:
  type: object
  required: [providerId, fields]
  properties:
    providerId: {type: string}
    fields:
      type: object
      additionalProperties: true

ApiResponseProfileSectionList:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data:
      type: array
      items: {$ref: "#/ProfileSection"}

ApiResponseProfileSection:
  type: object
  required: [code, message, data]
  properties:
    code: {type: integer}
    message: {type: string}
    data: {$ref: "#/ProfileSection"}
