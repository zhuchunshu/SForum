ApiResponseExtension:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/Extension"
ApiResponseExtensionInstallResult:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ExtensionInstallResult"
ApiResponseExtensionUninstall:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: object
      required:
      - uninstalled
      - extensionId
      properties:
        uninstalled:
          type: boolean
        extensionId:
          type: string
ApiResponseExtensionMigrationList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ExtensionMigrationRecord"
ApiResponseExtensionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/Extension"
ApiResponseExtensionEventList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ExtensionEvent"
ApiResponseExtensionEventDefinitionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ExtensionEventDefinition"
ApiResponseExtensionEventDeliveryList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ExtensionEventDelivery"
ApiResponseExtensionNavigationList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ExtensionAdminNavigationItem"
ApiResponseContributionPointDefinitionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/ContributionPointDefinition"
ApiResponseEffectiveContributionList:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      type: array
      items:
        "$ref": "#/EffectiveContribution"
ApiResponseExtensionSettings:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ExtensionSettings"
ApiResponseExtensionSettingsActionResult:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/ExtensionSettingsActionResult"
ApiResponseFrontendTrustChallenge:
  type: object
  required: [code, message, data]
  properties:
    code: { type: integer }
    message: { type: string }
    data:
      "$ref": "#/FrontendTrustChallenge"
ExtensionType:
  type: string
  enum:
  - plugin
  - theme
ExtensionStatus:
  type: string
  enum:
  - installed
  - enabled
  - disabled
ExtensionSource:
  type: string
  enum:
  - builtin
  - uploaded
ExtensionManifest:
  type: object
  description: SForum extension manifest. Plugins may declare runtime, route, provider, settings, event, and admin capabilities. Themes are buildless theme.json/static-asset packages; they may declare settings and admin pages but must not declare plugin runtime capabilities.
  required:
  - id
  - name
  - description
  - url
  - author
  - version
  - type
  - sforumVersion
  properties:
    id:
      type: string
    name:
      type: string
    description:
      type: string
    url:
      type: string
      format: uri
    author:
      "$ref": "#/ExtensionManifestAuthor"
    version:
      type: string
    type:
      "$ref": "#/ExtensionType"
    sforumVersion:
      type: string
    langs:
      type: object
      description: Optional localized display overrides. Top-level name, description, url, and author are the default English copy. When langs is omitted, hosts use the top-level fields as-is. Locale keys may be short codes such as zh or full tags such as zh-CN; UI locales like zh-CN also match zh.
      additionalProperties:
        "$ref": "#/ExtensionManifestLocale"
    permissions:
      type: array
      description: Plugin-only permission keys. Theme manifests must omit permissions in v1.
      items:
        type: string
    capabilities:
      type: array
      description: >-
        Plugin-only host capability keys (F2.1). Must be entries from the host
        capability catalog (e.g. net.outbound, jobs.enqueue, host.api). Themes
        must omit capabilities. When omitted, the host may still imply a
        minimal set from backend/jobs/settings/providers.
      items:
        type: string
    settings:
      description: Versioned Settings Document, or the legacy array shorthand normalized to Schema/form.
      oneOf:
      - type: array
        items:
          "$ref": "#/ExtensionManifestSetting"
      - "$ref": "#/ExtensionManifestSettingsDocument"
    migrations:
      type: array
      description: Plugin-only SQL migration declarations. Theme manifests must omit migrations in v1.
      items:
        type: object
    backend:
      type: object
      description: Plugin-only backend runtime declaration. Theme manifests must omit backend runtime fields in v1.
      properties:
        entry:
          type: string
        rpc:
          type: string
        protocolVersion:
          type: integer
    admin:
      "$ref": "#/ExtensionManifestAdmin"
    adminPages:
      type: array
      description: Legacy core-container admin page declarations. Prefer admin.pages for new manifests; legacy pages remain compatibility-mapped.
      items:
        "$ref": "#/ExtensionManifestAdminPage"
    routes:
      type: array
      description: Plugin-only proxied route declarations. Theme manifests must omit routes in v1.
      items:
        "$ref": "#/ExtensionManifestRoute"
    hooks:
      type: array
      description: Plugin-only lifecycle or domain hook declarations. Theme manifests must omit hooks in v1.
      items:
        "$ref": "#/ExtensionManifestHook"
    events:
      type: array
      description: Plugin-only event listener declarations. Theme manifests must omit events in v1. Legacy hooks are treated as observe/filter listeners according to the host event catalog.
      items:
        "$ref": "#/ExtensionManifestEvent"
    jobs:
      type: array
      description: Plugin-only background job declarations. Theme manifests must omit jobs in v1.
      items:
        type: object
    providers:
      type: array
      description: Plugin-only provider slot declarations. Theme manifests must omit providers in v1.
      items:
        "$ref": "#/ExtensionManifestProvider"
    contributions:
      type: array
      description: Plugin-only declarative contributions to host-owned typed points. Theme manifests must omit contributions in v1.
      items:
        "$ref": "#/ExtensionManifestContribution"
ExtensionManifestAuthor:
  type: object
  required:
  - name
  properties:
    name:
      type: string
    url:
      type: string
      format: uri
    email:
      type: string
      format: email
ExtensionManifestLocale:
  type: object
  description: Localized display fields for one locale. All properties are optional; present non-empty values override the matching top-level default English field.
  properties:
    name:
      type: string
    description:
      type: string
    url:
      type: string
      format: uri
    author:
      type: object
      properties:
        name:
          type: string
        url:
          type: string
          format: uri
        email:
          type: string
          format: email
# 设置展示文案：纯字符串，或 locale → 文案 map（如 zh-CN / en-US）。
LocalizedText:
  oneOf:
  - type: string
  - type: object
    additionalProperties:
      type: string
    minProperties: 1
    description: Locale map. Keys are BCP 47 codes such as zh-CN or en-US.
ExtensionManifestSetting:
  type: object
  required:
  - key
  - label
  - type
  properties:
    key:
      type: string
    label:
      "$ref": "#/LocalizedText"
    description:
      "$ref": "#/LocalizedText"
    type:
      type: string
      description: Host-rendered control type. Supported values are text, string, number, boolean, select, secret, and textarea.
      enum: [text, string, number, boolean, select, secret, textarea]
    default:
      type: string
    placeholder:
      "$ref": "#/LocalizedText"
      description: Optional example or input hint rendered by the host after locale resolution.
    recommendedValue:
      type: string
      description: Provider-declared value used by the generic restore-recommended action. Secrets are never restored or cleared through this field.
    width:
      type: string
      description: Schema UI control width. default caps the control (max-w-xl); full fills the available column width. Omitted values are treated as default.
      enum: [default, full]
    group:
      "$ref": "#/LocalizedText"
      description: Optional section title used to group related controls in declaration order.
    groupId:
      type: string
      description: Optional Settings Document group id. When omitted, the host may match group label text to a declared group.
    column:
      type: integer
      minimum: 1
      maximum: 2
      description: Optional column hint inside a two-column group.
    options:
      type: array
      description: Ordered choices. Settings with options render as a select instead of free text.
      items:
        type: object
        required: [value, label]
        properties:
          value:
            type: string
          label:
            "$ref": "#/LocalizedText"
          description:
            "$ref": "#/LocalizedText"
ExtensionManifestSettingsDocument:
  type: object
  required: [schemaVersion, ui, fields]
  properties:
    schemaVersion:
      type: integer
      enum: [1]
    ui:
      "$ref": "#/ExtensionManifestSettingsUI"
    fields:
      type: array
      items:
        "$ref": "#/ExtensionManifestSetting"
    actions:
      type: array
      items:
        "$ref": "#/ExtensionManifestSettingsAction"
ExtensionManifestSettingsUI:
  type: object
  required: [mode, layout]
  properties:
    mode:
      type: string
      enum: [schema, component]
    layout:
      type: string
      enum: [form, tabs]
    tabs:
      type: array
      items:
        "$ref": "#/ExtensionManifestSettingsTab"
    groups:
      type: array
      items:
        "$ref": "#/ExtensionManifestSettingsGroup"
    callouts:
      type: array
      items:
        "$ref": "#/ExtensionManifestSettingsCallout"
    component:
      "$ref": "#/ExtensionManifestSettingsComponent"
ExtensionManifestSettingsTab:
  type: object
  required: [id, label]
  properties:
    id: { type: string }
    label: { "$ref": "#/LocalizedText" }
    description: { "$ref": "#/LocalizedText" }
    groups:
      type: array
      items: { type: string }
ExtensionManifestSettingsGroup:
  type: object
  required: [id, label]
  properties:
    id: { type: string }
    label: { "$ref": "#/LocalizedText" }
    description: { "$ref": "#/LocalizedText" }
    columns: { type: integer, minimum: 1, maximum: 2 }
ExtensionManifestSettingsCallout:
  type: object
  required: [id, title]
  properties:
    id: { type: string }
    tone: { type: string }
    title: { "$ref": "#/LocalizedText" }
    body: { "$ref": "#/LocalizedText" }
    tab: { type: string }
    group: { type: string }
ExtensionManifestSettingsComponent:
  type: object
  description: Author-prebuilt browser module. The host never compiles source files.
  required: [id, apiVersion, entry]
  properties:
    id: { type: string }
    apiVersion: { type: integer, enum: [1] }
    entry: { type: string, pattern: '^frontend/admin/dist/.+\.mjs$' }
    css: { type: string, pattern: '^frontend/admin/dist/.+\.css$' }
ExtensionManifestSettingsAction:
  type: object
  required: [id, kind, label]
  properties:
    id: { type: string }
    kind: { type: string, enum: [provider_probe] }
    label: { "$ref": "#/LocalizedText" }
    description: { "$ref": "#/LocalizedText" }
    placement: { type: string, enum: [header, footer], default: footer }
    useDraftValues: { type: boolean, default: false }
    fields:
      type: array
      items: { type: string }
ExtensionManifestAdmin:
  type: object
  description: Extension admin entry and page declarations. Manage resolves to entry, then settings, then first declared page, then the generated about page.
  properties:
    entry:
      type: string
      description: In-admin extension page path such as /settings. Must point to /about or a declared page.
    pages:
      type: array
      items:
        "$ref": "#/ExtensionManifestAdminPage"
ExtensionManifestAdminPage:
  type: object
  required:
  - path
  - label
  properties:
    path:
      type: string
    label:
      type: string
    description:
      type: string
    icon:
      type: string
    view:
      type: string
      enum:
      - about
      - settings
    menu:
      type: boolean
      default: false
      description: Whether this page appears in admin sidebar navigation. Defaults to false.
    order:
      type: integer
      minimum: 0
    permission:
      type: string
ExtensionManifestRoute:
  type: object
  required:
  - path
  - methods
  properties:
    path:
      type: string
    methods:
      type: array
      items:
        type: string
        enum:
        - GET
        - HEAD
        - OPTIONS
        - POST
        - PUT
        - PATCH
        - DELETE
    access:
      type: string
      enum:
      - public
      - login
      - permission
    permission:
      type: string
    timeoutMs:
      type: integer
      minimum: 0
ExtensionManifestHook:
  type: object
  required:
  - name
  properties:
    name:
      type: string
ExtensionManifestEvent:
  type: object
  required:
  - name
  properties:
    name:
      type: string
    kind:
      type: string
      enum:
      - observe
      - validate
      - filter
    timeoutMs:
      type: integer
      minimum: 0
ExtensionManifestProvider:
  type: object
  required:
  - slot
  - label
  properties:
    slot:
      type: string
    label:
      type: string
    timeoutMs:
      type: integer
      minimum: 0
ExtensionManifestContribution:
  type: object
  required:
  - point
  - id
  - label
  - payload
  properties:
    point:
      type: string
      enum:
      - forum.topic.actions
      - forum.topic.sidebar
      - forum.topic.badges
      - forum.comment.actions
      - forum.nav.items
      - forum.topic.list.badges
      - forum.composer.toolbar
      - forum.profile.tabs
      - admin.dashboard.widgets
      - system.health.checks
    id:
      type: string
      description: Unique contribution id within this extension and point.
    order:
      type: integer
      minimum: 0
      default: 0
    label:
      type: object
      additionalProperties:
        type: string
      description: Localized user-facing labels, typically zh-CN and en-US.
    icon:
      type: string
      description: Nuxt Icon name with approved i-lucide- or i-tabler- prefix.
    payload:
      "$ref": "#/ExtensionContributionPayload"
ExtensionContributionPayload:
  type: object
  description: >
    Host-owned contribution payloads (F4.3). Never executable JSON.
    Shape depends on the contribution point payloadType:
    extensionRoute, profileSection, topicSidebarCard, topicBadge, navItem,
    dashboardLink, or healthDescriptor.
  required:
  - type
  properties:
    type:
      type: string
      enum:
      - extensionRoute
      - hostLink
      - adminLink
      - extensionRuntime
      - static
    method:
      type: string
      enum:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
    path:
      type: string
      description: Safe plugin route path under /api/v1/extensions/{extensionId}; must start with / and must not target /api or external URLs.
    href:
      type: string
      description: In-app relative path for profile hostLink contributions.
    route:
      type: string
      description: Admin-shell relative route for dashboard widgets.
    severity:
      type: string
      enum:
      - info
      - success
      - warning
      - danger
    component:
      type: string
      description: Health component name for system.health.checks descriptors.
    confirm:
      type: boolean
    required:
      type: boolean
      description: For system.health.checks only; when true a failed component marks not_ready.
ExtensionRuntimeStatus:
  type: object
  required:
  - state
  - routeCount
  - hookCount
  - eventCount
  - providerCount
  properties:
    state:
      type: string
      enum:
      - stopped
      - starting
      - running
      - degraded
      - failed
      description: >-
        degraded means the plugin process is up but the host resilience gate
        has consecutive failures or an open circuit (F2.3).
    lastError:
      type: string
    startedAt:
      type: string
      format: date-time
    routeCount:
      type: integer
    hookCount:
      type: integer
    eventCount:
      type: integer
    providerCount:
      type: integer
    circuitOpen:
      type: boolean
      description: True when the host circuit breaker is open for this extension.
    circuitOpenUntil:
      type: string
      format: date-time
    consecutiveFailures:
      type: integer
    lastFailureReason:
      type: string
    lastFailureAt:
      type: string
      format: date-time
    activeRpcCalls:
      type: integer
    maxConcurrentRpc:
      type: integer
Extension:
  type: object
  required:
  - id
  - name
  - version
  - type
  - status
  - source
  - isSystem
  - isDeletable
  - manifest
  - packageDigest
  - adminFrontendDigest
  - packagePath
  - installedAt
  - updatedAt
  properties:
    id:
      type: string
    name:
      type: string
    version:
      type: string
    type:
      "$ref": "#/ExtensionType"
    status:
      "$ref": "#/ExtensionStatus"
    source:
      "$ref": "#/ExtensionSource"
    isSystem:
      type: boolean
    isDeletable:
      type: boolean
    manifest:
      "$ref": "#/ExtensionManifest"
    capabilityGrants:
      type: array
      description: >-
        Effective host capabilities for plugins (explicit manifest.capabilities
        plus host-implied grants). Used by the enable-time review UI (F2.1).
      items:
        "$ref": "#/ExtensionCapabilityGrant"
    runtime:
      "$ref": "#/ExtensionRuntimeStatus"
    packageDigest:
      type: string
      description: Canonical immutable package tree SHA-256 digest.
    adminFrontendDigest:
      type: string
      description: >-
        SHA-256 identity of the prebuilt admin component contract and bytes.
        Empty when the extension has no prebuilt admin UI.
    packagePath:
      type: string
      description: Server-local package path, visible only to extension managers.
    installedAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
ExtensionInstallResult:
  type: object
  required:
  - extension
  - upgraded
  properties:
    extension:
      "$ref": "#/Extension"
    upgraded:
      type: boolean
      description: True when the same extension id was already installed (F2.4 upgrade path).
    previousVersion:
      type: string
    previousDigest:
      type: string
    trustRevoked:
      type: boolean
      description: Frontend trust revoked because adminFrontendDigest changed.
    requiredReEnable:
      type: boolean
      description: Plugin must be enabled again after upgrade (status reset to installed).
ExtensionMigrationRecord:
  type: object
  required:
  - path
  - checksum
  - status
  - appliedAt
  properties:
    path:
      type: string
      description: Path relative to package root (manifest.migrations[].path).
    checksum:
      type: string
      description: SHA-256 of the migration file when readable.
    status:
      type: string
      enum:
      - recorded
      - applied
      - failed
      description: >-
        v1 host only records checksums (recorded); applied is reserved for a
        future executor; failed means the file was missing or unsafe.
    appliedAt:
      type: string
      format: date-time
    message:
      type: string
ExtensionAdminNavigationItem:
  type: object
  required:
  - extensionId
  - extensionName
  - extensionType
  - extensionStatus
  - path
  - label
  - description
  - icon
  - view
  - order
  properties:
    extensionId:
      type: string
    extensionName:
      type: string
    extensionType:
      "$ref": "#/ExtensionType"
    extensionStatus:
      "$ref": "#/ExtensionStatus"
    path:
      type: string
    label:
      type: string
    description:
      type: string
    icon:
      type: string
    view:
      type: string
      enum:
      - about
      - settings
    order:
      type: integer
ContributionPointDefinition:
  type: object
  required:
  - id
  - owner
  - kind
  - description
  - payloadType
  properties:
    id:
      type: string
    owner:
      type: string
    kind:
      type: string
    description:
      type: string
    payloadType:
      type: string
      enum:
      - extensionRoute
      - profileSection
      - topicSidebarCard
      - topicBadge
      - navItem
      - dashboardLink
      - healthDescriptor
EffectiveContribution:
  type: object
  required:
  - extensionId
  - extensionName
  - extensionType
  - point
  - id
  - order
  properties:
    extensionId:
      type: string
    extensionName:
      type: string
    extensionType:
      "$ref": "#/ExtensionType"
    point:
      type: string
    id:
      type: string
    order:
      type: integer
    label:
      type: object
      additionalProperties:
        type: string
    icon:
      type: string
    payload:
      "$ref": "#/ExtensionContributionPayload"
PublicActiveThemeSettings:
  type: object
  required:
  - themeId
  - settings
  properties:
    themeId:
      type: string
      description: Active theme extension id; empty when no theme is active.
    settings:
      type: object
      additionalProperties:
        type: string
      description: Non-secret setting key/value map (defaults applied when unset).
ApiResponsePublicActiveThemeSettings:
  type: object
  required:
  - data
  properties:
    data:
      "$ref": "#/PublicActiveThemeSettings"
ExtensionSettings:
  type: object
  required:
  - extensionId
  - extensionType
  - extensionVersion
  - extensionStatus
  - renderer
  - items
  properties:
    extensionId:
      type: string
    extensionType:
      "$ref": "#/ExtensionType"
    extensionVersion:
      type: string
    extensionStatus:
      "$ref": "#/ExtensionStatus"
    renderer:
      "$ref": "#/ExtensionSettingsRenderer"
    tabs:
      type: array
      items:
        "$ref": "#/ExtensionSettingsTab"
    groups:
      type: array
      items:
        "$ref": "#/ExtensionSettingsGroup"
    callouts:
      type: array
      items:
        "$ref": "#/ExtensionSettingsCallout"
    items:
      type: array
      items:
        "$ref": "#/ExtensionSettingValue"
    actions:
      type: array
      items:
        "$ref": "#/ExtensionSettingsAction"
ExtensionSettingsRenderer:
  type: object
  required: [mode, layout, source, fallback]
  properties:
    mode:
      type: string
      enum: [schema, component]
    layout:
      type: string
      enum: [form, tabs]
    source:
      type: string
      enum: [document, legacy_array]
    fallback:
      type: string
      enum: [schema]
    component:
      "$ref": "#/ExtensionSettingsComponent"
ExtensionSettingsComponent:
  type: object
  required: [id, kind, apiVersion]
  properties:
    id: { type: string }
    kind:
      type: string
      enum: [prebuilt]
    apiVersion: { type: integer, minimum: 1 }
    entry: { type: string }
    css: { type: string }
ExtensionSettingsTab:
  type: object
  required: [id, label]
  properties:
    id: { type: string }
    label: { type: string }
    description: { type: string }
    groups:
      type: array
      items: { type: string }
ExtensionSettingsGroup:
  type: object
  required: [id, label]
  properties:
    id: { type: string }
    label: { type: string }
    description: { type: string }
    columns: { type: integer, minimum: 1, maximum: 2 }
ExtensionSettingsCallout:
  type: object
  required: [id, tone, title]
  properties:
    id: { type: string }
    tone: { type: string }
    title: { type: string }
    body: { type: string }
    tab: { type: string }
    group: { type: string }
ExtensionSettingsAction:
  type: object
  required: [id, kind, label, placement, useDraftValues, available]
  properties:
    id: { type: string }
    kind:
      type: string
      enum: [provider_probe]
    label: { type: string }
    description: { type: string }
    placement: { type: string, enum: [header, footer] }
    useDraftValues: { type: boolean }
    fields:
      type: array
      items: { type: string }
    available: { type: boolean }
    unavailableReason: { type: string }
ExtensionSettingOption:
  type: object
  required:
  - value
  - label
  properties:
    value:
      type: string
    label:
      type: string
    description:
      type: string
ExtensionSettingValue:
  type: object
  description: Resolved presentation for one manifest setting. Label, description, placeholder, group, and option labels are strings already resolved for the request Accept-Language locale. Manifest may declare those fields as a plain string or a locale map; the API never returns the raw map here.
  required:
  - key
  - label
  - description
  - type
  - default
  - value
  properties:
    key:
      type: string
    label:
      type: string
    description:
      type: string
    type:
      type: string
      description: Host-rendered control type. Supported values are text, string, number, boolean, select, secret, and textarea.
      enum: [text, string, number, boolean, select, secret, textarea]
    default:
      type: string
    value:
      type: string
    placeholder:
      type: string
    recommendedValue:
      type: string
    width:
      type: string
      description: Schema UI control width resolved from the manifest. default caps the control; full fills the available column width.
      enum: [default, full]
    group:
      type: string
    groupId:
      type: string
    column:
      type: integer
      minimum: 1
      maximum: 2
    options:
      type: array
      items:
        "$ref": "#/ExtensionSettingOption"
    secretSet:
      type: boolean
ExtensionSettingsInput:
  type: object
  required:
  - values
  properties:
    values:
      type: object
      additionalProperties:
        type: string
ExtensionSettingsActionInput:
  type: object
  properties:
    values:
      type: object
      additionalProperties:
        type: string
      description: Draft values for non-secret declared fields only.
    secrets:
      type: object
      additionalProperties:
        "$ref": "#/ExtensionSettingsActionSecretInput"
ExtensionSettingsActionSecretInput:
  type: object
  required:
  - mode
  properties:
    mode:
      type: string
      enum:
      - preserve
      - replace
    value:
      type: string
      description: Required by host policy when mode=replace; never echoed or audited.
ExtensionSettingsActionResult:
  type: object
  required:
  - success
  - reason
  - message
  - durationMs
  properties:
    success:
      type: boolean
    reason:
      type: string
    message:
      type: string
    details:
      type: object
      additionalProperties:
        type: string
    suggestions:
      type: array
      items:
        type: string
    durationMs:
      type: integer
      format: int64
ExtensionEvent:
  type: object
  required:
  - id
  - extensionId
  - actorUserId
  - action
  - message
  - createdAt
  properties:
    id:
      type: integer
      format: int64
    extensionId:
      type: string
    actorUserId:
      type: integer
      format: int64
    action:
      type: string
      enum:
      - installed
      - builtin_synced
      - verified
      - enabled
      - enable_failed
      - disabled
      - theme_activated
      - theme_activation_queued
    message:
      type: string
    createdAt:
      type: string
      format: date-time
ExtensionEventDefinition:
  # Host event catalog entry. F1.3 documents timeoutMs + failurePolicy.
  # Sync filters enforce timeout via host context; heavy work must enqueue jobs.
  type: object
  required:
  - name
  - kind
  - description
  - timeoutMs
  - failurePolicy
  properties:
    name:
      type: string
    kind:
      type: string
      enum:
      - observe
      - validate
      - filter
    description:
      type: string
    payloadFields:
      type: array
      items:
        type: string
    patchFields:
      type: array
      items:
        type: string
    timeoutMs:
      type: integer
      description: Per-listener invoke limit in milliseconds.
    failurePolicy:
      type: string
      enum:
      - fail_closed
      - fail_open
      description: |
        For filter/validate: fail_closed rejects the host operation when a
        listener fails or times out (default). fail_open records the failure
        and continues. Observe events are non-blocking (fail_open semantics).
ExtensionEventDelivery:
  type: object
  required:
  - id
  - extensionId
  - eventName
  - eventKind
  - status
  - reason
  - message
  - correlationId
  - attemptCount
  - createdAt
  - updatedAt
  properties:
    id:
      type: integer
      format: int64
    extensionId:
      type: string
    eventName:
      type: string
    eventKind:
      type: string
      enum:
      - observe
      - validate
      - filter
    status:
      type: string
      enum:
      - queued
      - running
      - succeeded
      - failed
      - skipped
    reason:
      type: string
    message:
      type: string
    correlationId:
      type: string
    attemptCount:
      type: integer
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    completedAt:
      type: string
      format: date-time
GrantFrontendTrustInput:
  type: object
  required:
  - digest
  properties:
    digest:
      type: string
      pattern: '^[0-9a-f]{64}$'
      description: >-
        Current adminFrontendDigest. The host binds the grant to extension id,
        version, API version, component identity, and these exact bytes.
    confirmation:
      "$ref": "#/FrontendTrustConfirmation"
FrontendTrustConfirmation:
  type: object
  required: [challengeId, code, extensionId, version, digest, apiVersion, componentId, phrase, acknowledged]
  properties:
    challengeId: { type: string, pattern: '^[0-9a-f]{32}$' }
    code: { type: string, pattern: '^[0-9]{6}$' }
    extensionId: { type: string }
    version: { type: string }
    digest: { type: string, pattern: '^[0-9a-f]{64}$' }
    apiVersion: { type: integer, minimum: 1 }
    componentId: { type: string }
    phrase:
      type: string
      description: Must exactly equal extensionId to prove intentional approval.
    acknowledged:
      type: boolean
      description: Administrator acknowledges that component code is fully trusted, not sandboxed.
FrontendTrustChallenge:
  type: object
  required: [challengeId, code, extensionId, version, digest, apiVersion, componentId, expiresAt]
  properties:
    challengeId: { type: string, pattern: '^[0-9a-f]{32}$' }
    code: { type: string, pattern: '^[0-9]{6}$' }
    extensionId: { type: string }
    version: { type: string }
    digest: { type: string, pattern: '^[0-9a-f]{64}$' }
    apiVersion: { type: integer, minimum: 1 }
    componentId: { type: string }
    expiresAt: { type: string, format: date-time }
FrontendTrustState:
  type: string
  enum:
  - none
  - required
  - trusted
  - source_trusted
  - revoked
  - invalidated
FrontendStatus:
  type: object
  required:
  - extensionId
  - kind
  - trustState
  properties:
    extensionId:
      type: string
    kind:
      type: string
      enum: [none, prebuilt_component]
    component:
      "$ref": "#/PrebuiltSettingsComponent"
    trustState:
      "$ref": "#/FrontendTrustState"
    digest:
      type: string
      description: Current adminFrontendDigest presented for explicit trust.
PrebuiltSettingsComponent:
  type: object
  required: [id, apiVersion, entry]
  properties:
    id: { type: string }
    apiVersion: { type: integer, minimum: 1 }
    entry: { type: string, pattern: '^frontend/admin/dist/.+\.mjs$' }
    css: { type: string, pattern: '^frontend/admin/dist/.+\.css$' }
ExtensionCapabilityGrant:
  type: object
  required:
  - key
  - risk
  - labelZh
  - labelEn
  - description
  properties:
    key:
      type: string
    risk:
      type: string
      enum: [low, medium, high]
    labelZh:
      type: string
    labelEn:
      type: string
    description:
      type: string
    implied:
      type: boolean
      description: True when the host inferred this grant (not explicitly listed in manifest.capabilities).
ApiResponseFrontendStatus:
  type: object
  required:
  - code
  - message
  - data
  properties:
    code:
      type: integer
    message:
      type: string
    data:
      "$ref": "#/FrontendStatus"
